import { hoursAgo } from '../../../helpers/hours-ago';
import { module, test } from 'qunit';

module('Unit | Helper | hours ago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hoursAgo(42);
  assert.ok(result);
});
