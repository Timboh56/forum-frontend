import { labelIsActive } from '../../../helpers/label-is-active';
import { module, test } from 'qunit';

module('Unit | Helper | label is active');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = labelIsActive(42);
  assert.ok(result);
});
