import { registered } from '../../../helpers/registered';
import { module, test } from 'qunit';

module('Unit | Helper | registered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = registered(42);
  assert.ok(result);
});
