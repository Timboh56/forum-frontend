import { removeDashes } from '../../../helpers/remove-dashes';
import { module, test } from 'qunit';

module('Unit | Helper | remove dashes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removeDashes(42);
  assert.ok(result);
});
