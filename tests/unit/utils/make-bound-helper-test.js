import makeBoundHelper from '../../../utils/make-bound-helper';
import { module, test } from 'qunit';

module('Unit | Utility | make bound helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeBoundHelper();
  assert.ok(result);
});
