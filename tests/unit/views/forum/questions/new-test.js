import { moduleFor, test } from 'ember-qunit';

moduleFor('view:forum/questions/new', 'Unit | View | forum/questions/new');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
